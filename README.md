# Flask + MongoDB Atlas Application

This project includes:
- Flask API returning JSON from a file
- MongoDB Atlas integration with PyMongo
- Form submission and success/error handling

## Run locally:
pip install -r requirements.txt
python app.py

## MongoDB Connection:
Configured via .env file (not included in repo)
